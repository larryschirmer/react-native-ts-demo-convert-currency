import { Context as CurrencyContext } from './currency';
import { Provider as CurrencyProvider } from './currency';

export const Currency = {
  Context: CurrencyContext,
  Provider: CurrencyProvider,
};
