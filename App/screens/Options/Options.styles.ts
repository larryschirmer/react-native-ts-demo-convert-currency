import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    flex: 1,
  },
  chevIcon: {
    width: 15,
    height: 15,
  },
  shareIcon: {
    width: 20,
    height: 20,
  },
});

export default styles;
